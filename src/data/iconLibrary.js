import {
  Star, Flag, Lightbulb, Target, Rocket, AlertCircle, CheckCircle, Heart,
  Zap, BookOpen, Briefcase, Calendar, Users, Globe, Home, Settings,
} from 'lucide-react';

// Name -> component map. Node data stores the string name (serializable);
// the node renderer looks it up here.
export const ICON_LIBRARY = {
  Star, Flag, Lightbulb, Target, Rocket, AlertCircle, CheckCircle, Heart,
  Zap, BookOpen, Briefcase, Calendar, Users, Globe, Home, Settings,
};

export const EMOJI_SET = [
  '💡', '🎯', '🚀', '✅', '⭐', '🔥', '📌', '📅',
  '💬', '⚠️', '❤️', '🧩', '📈', '🔧', '🎨', '📎',
];
